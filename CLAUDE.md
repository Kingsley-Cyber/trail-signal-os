# Claude Code Instructions

Read and obey `AGENTS.md` as the canonical repository contract. Before editing, run `niche-research validate`. Keep evidence append-only, use stable IDs, and never invent market metrics. Use the JSON Schemas in `schemas/` for structured outputs and run the test suite after changes.
