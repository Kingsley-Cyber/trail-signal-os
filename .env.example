# Copy to .env (gitignored). Never commit real values.
POSTGRES_PASSWORD=REPLACE_ME
# Optional overrides (defaults match docker-compose.yml)
# POSTGRES_USER=trail_signal
# POSTGRES_DB=trail_signal
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5433
# graphify semantic-extraction backend (docs/papers passes; code passes need no key)
DEEPSEEK_API_KEY=sk-REPLACE_ME
# Optional alternative graphify backend
# GEMINI_API_KEY=
